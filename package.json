{
  "name": "@mdhnpm/aws-ssm-parameters",
  "version": "1.0.3",
  "description": "Node module to get a list of parameters from AWS Parameter Store",
  "main": "src/get-parameters.js",
  "scripts": {
    "test": "nyc --reporter=html --reporter=text mocha **/test/**/*.spec.js --recursive",
    "test:watch": "nyc --reporter=html --reporter=text mocha **/test/**/*.spec.js --recursive --watch"
  },
  "url": "https://github.com/mydatahack/aws-ssm-parameters",
  "keywords": [
    "aws",
    "ssm",
    "parameter store",
    "get parameters"
  ],
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.496.0"
  },
  "devDependencies": {
    "aws-sdk-mock": "^4.5.0",
    "babel-eslint": "^10.0.2",
    "chai": "^4.2.0",
    "eslint": "^6.1.0",
    "mocha": "^10.2.0",
    "nyc": "^14.1.1",
    "sinon": "^7.3.2"
  }
}
